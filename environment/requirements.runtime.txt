# The base image already contains the vLLM-coupled Torch/CUDA stack.
# These direct dependencies are pinned; the completed image records the full
# transitive lock in environment/pip_freeze.txt at runtime.
vllm==0.11.0
transformers==4.57.6
datasets==4.5.0
huggingface-hub==0.34.4
math-verify==0.9.0
PyYAML==6.0.2
numpy==2.2.6
pandas==2.2.3
# datasets 4.5.0 requires pyarrow>=21.0.0. Pin the first compatible release;
# it provides a CPython 3.12 manylinux wheel used by the vLLM 0.11 base image.
pyarrow==21.0.0
scipy==1.15.3
scikit-learn==1.6.1
matplotlib==3.10.1
sentence-transformers==3.4.1
pytest==8.3.5
ruff==0.11.2
